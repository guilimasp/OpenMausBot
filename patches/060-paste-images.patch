diff --git a/server/contracts.ts b/server/contracts.ts
index eb03bfd..b4d9283 100644
--- a/server/contracts.ts
+++ b/server/contracts.ts
@@ -139,6 +139,9 @@ export type RequestOutcome = "allowed-once" | "rejected" | "answered" | "unavail
 export interface SendTurnInput {
   threadId: ThreadId;
   text: string;
+  /** Images the user attached (base64). Drivers that cannot pass images to
+   * their agent ignore them and send the text alone. */
+  images?: Array<{ mime: string; data: string; name?: string }>;
   model?: string;
   effort?: EffortLevel;
   resumeCursor?: unknown;
diff --git a/server/drivers/claude.ts b/server/drivers/claude.ts
index e00da8f..10e2a51 100644
--- a/server/drivers/claude.ts
+++ b/server/drivers/claude.ts
@@ -13,6 +13,7 @@ import { createServer as createNetServer } from "node:net";
 import { homedir, tmpdir } from "node:os";
 import { join, dirname } from "node:path";
 
+import { userContent } from "../attachments.ts";
 import { DATA_DIR } from "../config.ts";
 import { augmentedPath } from "../env-path.ts";
 import { brokerSocketPath, describeSpawnFailure, execCli, killCliTree, spawnCli } from "../procs.ts";
@@ -617,7 +618,9 @@ export const ClaudeDriver: ProviderDriver<ClaudeConfig> = {
       emit({ ...base(threadId, turnId), type: "turn.started" });
 
       // prompt over stdin as a stream-json message — never argv (ARG_MAX)
-      const promptMsg = { type: "user", message: { role: "user", content: turn.text } };
+      // attached images ride along as Messages-API image blocks; with none,
+      // content stays the plain string upstream sends
+      const promptMsg = { type: "user", message: { role: "user", content: userContent(turn.text, turn.images) } };
       child.stdin.write(JSON.stringify(promptMsg) + "\n");
       child.stdin.end();
       appendNative(threadId, { dir: "out", source: "claude.sdk.message", msg: promptMsg });
diff --git a/server/index.ts b/server/index.ts
index ee3fe84..07cc0fe 100644
--- a/server/index.ts
+++ b/server/index.ts
@@ -9,6 +9,7 @@ import { extname, join } from "node:path";
 
 import { z } from "zod";
 
+import { sanitizeImages, type ImageAttachment } from "./attachments.ts";
 import { approvalKey, autoDecision } from "./auto-approve.ts";
 import { validateBotCwd } from "./bot-cwd.ts";
 import { groupTurnCwd } from "./room-cwd.ts";
@@ -1067,6 +1068,9 @@ async function startTurn(
   opts?: {
     commsDepth?: number;
     userMessage?: Message;
+    /** Images pasted or dropped into the composer, on their way to a driver
+     * that can show them to its agent (see server/attachments.ts). */
+    images?: ImageAttachment[];
     /** Routines run in detached tasks; pin the destination for the whole turn. */
     threadId?: string;
     /** Cloud routines run the whole agent inside the bot's Box VM instead
@@ -1130,7 +1134,12 @@ async function startTurn(
   if (!userMessage) {
     userMessage = opts?.connectorContinuation
       ? { id: `connector-${randomUUID()}`, at: Date.now(), role: "user", kind: "text", text }
-      : store.appendMessage(threadId, { role: "user", kind: "text", text });
+      : store.appendMessage(threadId, {
+          role: "user",
+          kind: "text",
+          text,
+          ...(opts?.images?.length ? { images: opts.images } : {}),
+        });
   }
 
   // transcript for API-backed drivers: settled text turns on the ACTIVE
@@ -1331,6 +1340,7 @@ async function startTurn(
       await instance.adapter.sendTurn({
         threadId,
         text: turnText,
+        images: opts?.images?.length ? opts.images : undefined,
         model,
         effort,
         // a rewound thread never resumes the abandoned branch's session
@@ -2932,7 +2942,10 @@ const server = createServer(async (req, res) => {
     if (m && method === "POST") {
       const body = await readBody(req);
       const text = String(body.text ?? "").trim();
-      if (!text) return json(res, 400, { error: "text required" });
+      // a pasted screenshot is a message on its own — anything malformed or
+      // oversized is dropped here rather than costing the user their send
+      const images = sanitizeImages(body.images);
+      if (!text && images.length === 0) return json(res, 400, { error: "text required" });
       const bot = store.bot(m[1]);
       if (!bot) return json(res, 404, { error: "no such bot" });
       // A busy bot no longer refuses the message: it lands in the thread
@@ -2943,7 +2956,9 @@ const server = createServer(async (req, res) => {
         const message = queueSteeredMessage(store, bot, text);
         return json(res, 202, { ok: true, queued: true, messageId: message.id });
       }
-      await startTurn(bot.id, text);
+      // the steer queue above carries text only, so a message with images
+      // reaches this line only when the bot is free to take it now
+      await startTurn(bot.id, text, { images });
       return json(res, 202, { ok: true });
     }
 
diff --git a/server/store.ts b/server/store.ts
index ca2d0f8..f0b9adc 100644
--- a/server/store.ts
+++ b/server/store.ts
@@ -80,6 +80,8 @@ export interface Message {
   /** screen messages: a frame of the bot's computer (base64 image) */
   png?: string;
   mime?: string;
+  /** images the user attached to this message (base64) */
+  images?: Array<{ mime: string; data: string; name?: string }>;
   at: number;
   /** the message this one follows; null = thread root. Edited messages
    * share a parentId with the version they replace — that's a fork. */
diff --git a/src/components/ChatView.tsx b/src/components/ChatView.tsx
index 7e16cca..e0fb994 100644
--- a/src/components/ChatView.tsx
+++ b/src/components/ChatView.tsx
@@ -40,6 +40,7 @@ import { OptionCard } from "./OptionCard";
 import { ApprovalCard } from "./ApprovalCard";
 import { Composer } from "./Composer";
 import { ConnectorCard } from "./ConnectorCard";
+import { MessageImages } from "./Attachments";
 import { ModelPicker } from "./ModelPicker";
 import { RenameTitle } from "./RenameTitle";
 import { TaskPicker } from "./TaskPicker";
@@ -365,6 +366,7 @@ function Bubble({
                   Show less
                 </button>
               )}
+              {message.images?.length ? <MessageImages images={message.images} /> : null}
             </>
           ) : (
             <MessageBoundary fallbackText={text}>
diff --git a/src/components/Composer.tsx b/src/components/Composer.tsx
index 218831a..dc02b14 100644
--- a/src/components/Composer.tsx
+++ b/src/components/Composer.tsx
@@ -5,7 +5,9 @@ import { useStore, visibleMessages, type Bot, type Group } from "@/state/store";
 import { cn } from "@/lib/cn";
 import { useComposerDraft } from "@/lib/drafts";
 import { MausAvatar } from "./Avatar";
+import { AttachmentStrip } from "./Attachments";
 import { ComposerAttachments } from "./ComposerAttachments";
+import { hasImages, imagesFromClipboard, MAX_ATTACHMENTS, type Attachment as ImageAttachment } from "@/lib/images";
 import {
   composeMessage,
   isLongPaste,
@@ -74,6 +76,11 @@ export function Composer({
     (id: string) => setAttachments((prev) => prev.filter((a) => a.id !== id)),
     [setAttachments],
   );
+  // Pasted images, held apart from the chips above: a chip folds into the
+  // prompt text on send, while an image has to reach the driver as its own
+  // block. Deliberately not in the draft — a screenshot per bot kept alive
+  // across switches is megabytes of state nobody asked to keep.
+  const [images, setImages] = useState<ImageAttachment[]>([]);
   const [recording, setRecording] = useState(false);
   const [speechError, setSpeechError] = useState<string | null>(null);
   const [caret, setCaret] = useState(0);
@@ -134,10 +141,11 @@ export function Composer({
   // in the transcript immediately with a queued affordance.
   const [queued, setQueued] = useState<string | null>(null);
   // a chip on its own is a message: the send control has to appear for it
-  const hasContent = Boolean(text.trim()) || attachments.length > 0;
+  const hasContent = Boolean(text.trim()) || attachments.length > 0 || images.length > 0;
   const send = () => {
     const t = composeMessage(text, attachments);
-    if (!t) return;
+    // an image on its own is a message, even with nothing typed
+    if (!t && images.length === 0) return;
     if (busy && group) {
       setQueued(t);
       setText("");
@@ -148,11 +156,12 @@ export function Composer({
       dispatch({ type: "sendGroup", groupId: group.id, text: t });
       track("message_sent", { room: true });
     } else if (bot) {
-      dispatch({ type: "send", botId: bot.id, text: t });
+      dispatch({ type: "send", botId: bot.id, text: t, images: images.length ? images : undefined });
       track("message_sent", { driver: bot.modelSelection?.instanceId, queued: busy });
     }
     setText("");
     setAttachments([]);
+    setImages([]);
   };
   useEffect(() => {
     if (!busy && queued && group) {
@@ -284,6 +293,7 @@ export function Composer({
           onAdd={addAttachments}
           onRemove={removeAttachment}
         />
+        <AttachmentStrip images={images} onRemove={(i) => setImages((cur) => cur.filter((_, j) => j !== i))} />
         <div className="flex items-end gap-2 rounded-3xl border border-hairline/40 bg-raised/60 py-2 pl-3 pr-2">
         <textarea
           ref={inputRef}
@@ -295,6 +305,17 @@ export function Composer({
             setDismissedAt(null);
           }}
           onPaste={(e) => {
+            // an image goes to the model as an image. Checked first and
+            // synchronously: a screenshot copied from Finder puts its *name*
+            // on the clipboard as text too, and that must not land in the box
+            // next to the thumbnail.
+            if (hasImages(e.clipboardData)) {
+              e.preventDefault();
+              void imagesFromClipboard(e.clipboardData).then((pasted) => {
+                if (pasted.length) setImages((cur) => [...cur, ...pasted].slice(0, MAX_ATTACHMENTS));
+              });
+              return;
+            }
             // a wall of text becomes a chip instead of burying the input
             const pasted = e.clipboardData.getData("text/plain");
             if (!isLongPaste(pasted)) return;
diff --git a/src/state/store.tsx b/src/state/store.tsx
index fd2b724..5722674 100644
--- a/src/state/store.tsx
+++ b/src/state/store.tsx
@@ -64,6 +64,8 @@ export interface Message {
   /** screen messages: a frame of the bot's computer (base64) */
   png?: string;
   mime?: string;
+  /** images the user attached to this message (base64) */
+  images?: Array<{ mime: string; data: string; name?: string }>;
   at: number;
   /** the message this one follows; null = thread root. Edited messages
    * share a parentId with the version they replace — that's a fork. */
@@ -340,7 +342,7 @@ export type Action =
   | { type: "instances"; instances: InstanceInfo[] }
   | { type: "configStatus"; config: ConfigStatus }
   | { type: "select"; id: string }
-  | { type: "send"; botId: string; text: string }
+  | { type: "send"; botId: string; text: string; images?: Message["images"] }
   | { type: "editMessage"; botId: string; messageId: string; text: string }
   | { type: "switchBranch"; botId: string; messageId: string }
   | { type: "threadActive"; threadId: string; activeLeafId: string }
@@ -1000,7 +1002,7 @@ export function StoreProvider({ children }: { children: ReactNode }) {
         case "send":
           api(`/api/bots/${action.botId}/messages`, {
             method: "POST",
-            body: JSON.stringify({ text: action.text }),
+            body: JSON.stringify({ text: action.text, images: action.images }),
           }).catch(showError);
           break;
         case "editMessage":
